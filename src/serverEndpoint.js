<script>
    var ws = new WebSocket("");  // put path inside
    ws.onopen = function(message) { processOpen(message)};
    var mes = Document.getElementByID("message");
    function processOpen(message) {
        me.innerHtml = 
    }
    


</script>